usage: cdata [operation] [id]
operations:
    cdata add                <filters>
    cdata bookmark [options]           [id]
    cdata citation [options]           <id>
    cdata {help,-h,--help} [operation]
    cdata modify   [options] <filters> <id>
    cdata remove   [options]           <id>
    cdata search   [options] [filters] [id]
    cdata view     [options]           <id>
    cdata {version,-v,--version}

usage: cdata add <filters>
filters:
    -f <file>
    -t <title>
    -a <author1 [author2] ...>
    -k <keyword1 [keyword2] ...>
    -j <journal>
    -v <volume>
    -i <issue>
    -d <date>
    -p <page-from> <page-to>

usage: cdata search [filters] [id]
filters:
    -f <file>
    -t <title>
    -a <author1 [author2] ...>
    -k <keyword1 [keyword2] ...>
    -j <journal>
    -v <volume>
    -i <issue>
    -d <date-from> <date-to>
    -p <page-from> <page-to>
    -b : bookmarks
if no filters specified, specify <id> or nothing (everything will be printed)
view not yet implemented
